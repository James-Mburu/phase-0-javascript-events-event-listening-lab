function addingEventListener() {
input = document.getElementById('input');
            // Selecting the elelment "input"
function clickAlert() {
    //adding event listener and binding it
  alert('I was clicked!');
}

input.addEventListener('click', clickAlert);
}
